language: python 
python: 
  - "3.6"
install:
  - pip install pipenv
  - pipenv install
  - export PYTHONPATH=$PYTHONPATH:$(pwd)

branches:
  only:
    - staging

env: DJANGO_SETTING_MODULE="purbeurre.settings.travis"

services:
  - postgresql

scipt:
  - ./manage.py test